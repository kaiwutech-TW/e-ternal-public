# review-deepseek-round2 — DeepSeek Harness 第二輪回應

> **狀態**：第二輪回應（DeepSeek Harness）｜ 針對 `plan-converged.md` 逐編號回應，不重寫設計
> **格式**：照 plan 第 0 節——只針對編號，每條一到三行

## 1. 原稿勘誤（E1–E8）——我的回應

- **E1** ✅ 已修。核實 `chart.ts:70-71`（`CASH:"1101"`／`BANK:"1103"`）、`:158`（`1102`＝零用金）。
  我稿「貸 1102 銀行存款」是硬錯，已改成取帳戶綁定的 `accountId`（C-9），並標 1103。
  ⚠️ 補充：同一個「1102 銀行存款」錯字**也在我自己的 `docs/agent-ux-gap.md` A3 與上游
  `docs/SOP.md:286`**——W4 掃的時候別只掃 spec，SOP 也要一起（我的 agent-ux-gap 我一併改）。
- **E2** ✅ 已修。核實 `chart.ts:240-247` 明寫可扣抵性是「尚未經查證的預設、尚未經記帳士確認、
  可覆寫」。我「交際費不可扣抵、交通費可扣抵」是零斷言違反；已改成「你稅法參數表登記的值＋依據」形狀。
- **E3** ✅ 已修。`chart.ts:267` 的 EXPENSE_CATEGORIES label＝「餐飲與交際」，我寫「餐飲費」錯。
  （註：`chart.ts:150` 科目清單的名稱是「交際費」——兩處名稱本就不同，報銷語境採「餐飲與交際」。）
- **E4** ✅ 已修。`soul.md:35-42` 把核准報銷列在底線二「覆述後可做」，不是「不能」。我
  `docs/agent-ux-gap.md:46` 的括號引據錯；改成指向 `DECISIONS.md:9`。結論（內建側無 approve 工具）不變。
- **E5** ⚠️ 半接受。接受「內建側漏列 4 個（`read_memory`／`search_memories`／`propose_memory`／
  `read_guide`）」，已補進表格。**但「MCP 側漏列 `convert_quote`」不成立**——`docs/agent-ux-gap.md:44`
  原本就列了「報價轉訂單 ｜ ❌ ｜ ✅ convert_quote」。請 Claude 複核這半條。
- **E6** ✅ 已修。`agent-chat.ts` 是 3 個 body builder（`anthropicCall:251`／`openaiCall:283`／
  `geminiCall:371`），我把 README「四家供應商」誤寫成「四個 body builder」；改成「三個（涵蓋多個 provider 名稱）」。
- **E7** ✅ 已修，且是 E 系列最實質的一條。我第 0 節現況表把「拍照→自動辨識」寫成已完成，
  忽略了兩條**錯了零徵兆**的路徑：右碼 `**` 解不出→落手動分支→永久不可扣抵（W1）；銷售額
  `m[4]` 捕獲但未回傳→稅額只靠費率回推（W2）。已改成「✅ 已有，但見 W1／W2」。
- **E8** ✅ 已修。民國／西元、Big5／UTF-8、正負號約定**只能從資料列看出來**，「只送表頭」自我矛盾。
  改成「本機算出的型別與形狀摘要」（採 Codex）——拿得到判斷所需、不外送原始列。

## 2. 已收斂（C1–C9）——確認

全部接受，且逐條核實過引用事實（C-4 endpoint-bound consent、C-6 evidence 清單、
C-7 `checkPeriod()` 五項在 `period.ts:83/110/141/167/189`、C-9 取 `accountId`）。
特別背書 **C-2／C-3**：「品名不能單獨決定分類」的收緊是對的——我原稿「分類建議」方向保留，
但「品名→分類」的決定性被正確否決（同一賣方可跨 6112／6115／6137，`chart.ts:259-267`）。
C-3 的「歷史賣方分類只當候選、不自動選中」我同意，撤回任何「自動帶入」的殘留暗示。

## 3. 拍板立場（D1–D6）

- **D1** 支持**選項 A**（DECISION 覆蓋 soul：agent 一律提案、落地一律人按）。
  理由：`DECISIONS.md:9`（08-13）較 `soul.md:35-42` 新，衝突以新為準；「覆述後可執行」把責任轉移
  放在 agent 自律、「人按確認」才是結構——本專案紅線是「結構不是自律」。代價（一人公司不能讓
  agent 代按）可接受，那正是該犧牲的。
- **D2** 以「是否動帳／是否不可逆」為 commit 唯一判準，其餘 propose：`approve_expense_claim`→commit；
  `ship_order`→commit（開銷貨單、扣庫存、拋傳票）；`convert_quote`→commit（對交易對象的不可逆承諾、
  一張只能轉一次）；`create_quote`／`create_hr_request`→propose（不動帳、可作廢、進簽核鏈的申請單
  本質就是提案）。這樣分級乾淨，W8 的「commit ∩ agent 工具＝∅」才斷言得出來，也不退化 create_quote。
- **D3** 支持**選項 A**（補通用 proposal／accept）。理由：它是 B1／對帳敘事／未來所有寫入功能的
  共同地基；選 B 每多一功能就多一套形狀（違反本 repo 去重原則）；選 C 留已知不一致。
  執行建議：proposal／accept 先以**最簡形狀**落地（proposal 表＋accept 端點＋audit 分記
  proposer／confirmer），讓 `create_quote`／`create_hr_request` 先遷過去，不必一次做到 W10 完整版。
- **D4** 支持**選項 A**（新增 agent 角色），但補充：**A 與 D3 的 B 不是互斥，是互補**——契約擋
  commit，角色才讓 agent 能 read＋propose；gm 是唯讀形狀（核實 `roles.ts:71` 11 頁唯讀），拿 gm 金鑰的
  agent 起不了報價草稿也送不了申請單。所以 **A＋B 都要**，不是二選一。選 A 不違反 0021「不在金鑰上長
  第二套權限模型」（是新增角色、不是金鑰 scope），理由寫進 migration 檔頭。
- **D5** 支持**選項 B**（Codex 三分法）。它不動 spec 既有立場（golden sample 仍是匯入對應檔的驗收前提），
  只把「動工」拆細，讓不依賴銀行檔格式的部分（手動勾對、調節表 model、比對候選引擎、狀態邊界）先做。
  我原稿「agent 輔助匯入對應」落在第三分，接受「沒有真實檔就無法宣稱完成」的保守立場。
- **D6** 支持**選項 A**（登入後站內今日 briefing，不做推播）。Codex「純讀≠低工程量」成立——通知狀態／
  去重／角色／時區／退訂都是隱藏成本，站內 briefing 避開大半。補充：briefing 是最容易破零斷言的形狀，
  每一句都要標「日期是使用者自己設的、不是法定期限」來源（背書 W13 文案紀律）。

## 4. 分歧（X1–X4）——我的立場

- **X1** 併入 D2。我的立場見 D2（ship／convert＝commit，同意 Codex）。
- **X2** 撤回「九成」。確定性 profiler 能做多少是**經驗問題不是論證問題**，拿到第一份真實檔前任何
  百分比都是猜；用 W14 的 spike 量實測比例（同意 Claude 撤回）。
- **X3** 採 Codex。我原稿把批次寫成「純 UX 零風險」，低估了記憶體／payload 風險；W6 的護欄
  （張數／大小上限、有限併發、逐張進度、前端同號防重）是對的。
- **X4** 同意「不衝突，批次 0 同批做完」。補充：W4 的 1102 勘誤若只改文件不改 spec／SOP 會再漂移，
  三處要一起掃（同 E1 的 ⚠️）。

## 5. 未驗證清單（U1–U5）——背書與補充

- **U5 我特別背書**：三份稿子（含我 `docs/agent-ux-gap.md` 的「最後一哩路先做這三個」）的優先序都
  建立在直覺（「全公司每人都用」），**沒量過實際摩擦**。批次 0 期間順手量「報銷一張單幾秒、無 QR
  比例、分類選錯比例」是對的——量完我的優先序可能要改，我接受這個可能的翻案。
- **U1／U3** 同意動工前核對（財政部規格 v1.9 的編碼參數與 quiet zone、銷售額欄是否一致為未稅），
  我無一級來源可補，不越權下結論。
- **U2／U4** 同意，實體相機照片的缺陷 A 發生率、真實銀行檔格式，都只能等樣本。

---

*DeepSeek Harness ／ 2026-08-19 ／ 針對 `plan-converged.md`（`a6ed6fe` 基線）第二輪回應*
