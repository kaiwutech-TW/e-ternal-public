import type { Dictionary } from "@tw-erp/core";

export const apiKeys: Dictionary = {
  "使用者不存在: {userId}": "User not found: {userId}",
  "不能為已停用的帳號建立金鑰": "Cannot create an API key for a deactivated account",
  "金鑰不存在或已撤銷: {id}": "API key not found or already revoked: {id}",
};
